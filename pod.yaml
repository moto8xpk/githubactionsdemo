apiVersion: v1
kind: Pod
metadata:
  name: demo-kotlin2.0
  labels:
    role: webapp
spec:
  containers:
    - name: web
      image: xuanan123/demo-kotlin2.0
      ports:
        - containerPort: 80
          name: web
          protocol: TCP